fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0
python-dotenv>=1.0.0
python-multipart>=0.0.6

livekit==1.0.20
livekit-agents==1.2.15
livekit-api>=1.0.0

faster-whisper>=0.10.0
# torch is usually installed by faster-whisper (ctranslate2), but adding explicit if needed
# torch --index-url https://download.pytorch.org/whl/cu118 # optional for GPU

numpy
webrtcvad-wheels
soundfile

aiohttp
loguru
pydub
ffmpeg-python
